spec_idx = input(); // controllable via input

// Type1
if (spec_idx < ARRAY1_SIZE) {
  temp &= array2[array1[spec_idx] * 512];
}
// Type2
if (spec_idx < ARRAY1_SIZE) {
  if (spec_idx < ARRAY1_SIZE) {
    temp &= array2[array1[spec_idx] * 512];
  }
}
// Type3
if (spec_idx < ARRAY1_SIZE) {
  if (spec_idx < ARRAY1_SIZE) {
    if (spec_idx < ARRAY1_SIZE) {
      temp &= array2[array1[spec_idx] * 512];
    }
  }
}
